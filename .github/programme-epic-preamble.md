> **This body is GENERATED.** It is `scripts/programme-rollup.mjs --epic-body` — the same renderer,
> the same inputs, and the same bytes as `PROGRAMME.md`'s generated half. Do not edit it by hand:
> the next refresh overwrites it, and a hand-kept grid is a grid that lies the first time a run
> lands overnight. Refreshed daily at 06:07 UTC by `.github/workflows/programme.yml`, and by hand
> after each dispatch so it is live rather than daily.
>
> The programme's own entry point is `PROGRAMME.md` at the repo root. This issue exists so the grid
> is watchable from the board without cloning.

