{
	"$comment": [
		"The release gate's ratchet — GENERATED by scripts/e2e-ratchet.mjs --write; never hand-edited.",
		"BOOTSTRAP: no baseline has been captured yet. Every gate leg FAILS at its ratchet step until one is,",
		"deliberately — a gate that passes against an empty ledger is vacuous. Capture it from a labelled run",
		"(`release-gate:run` on the seams PR) once the Stripe test secrets exist: download each leg's",
		"results.json and run `node scripts/e2e-ratchet.mjs --project=<leg> --results=<json> --write`."
	],
	"version": 1,
	"bootstrap": true,
	"projects": {}
}
