---
title: alethia CLI
description: Go command-line tool for managing multi-cloud infrastructure via the Alethia platform.
---

# alethia CLI

alethia is the command-line interface for the platform. It provides terminal-based access to all infrastructure operations — creating zones, planning and deploying Specs, monitoring jobs, and managing Runners.

## Overview

| Property | Value |
|----------|-------|
| Language | Go |
| Framework | [Cobra](https://github.com/spf13/cobra) (commands) + [Charmbracelet](https://charm.sh/) (TUI) |
| Version | Managed by release-please (`alethia-v*` tags) |
| Distribution | Homebrew tap + GitHub Releases (darwin/linux, amd64/arm64) |

## How It Works

alethia is a **thin client** that communicates with the Alethia API over HTTPS. It does not run Terraform directly — instead, it queues jobs that [Runners](/runner) execute. The CLI handles:

1. **Authentication** — [Device code flow](/concepts/authentication) with tokens stored locally
2. **Configuration** — Creating and inspecting zones and Specs
3. **Job submission** — Queuing Plan / Deploy / Destroy jobs
4. **Monitoring** — Streaming logs, checking job status, waiting for completion
5. **Management** — Listing clusters, managing Runners

## Command Groups

<Cards>
  <Card title="Connector Commands" href="/cli/commands/connector">
    Connect AWS, GCP, and Azure cloud accounts.
  </Card>
  <Card title="Zone Commands" href="/cli/commands/zone">
    Create, list, and delete workspaces.
  </Card>
  <Card title="Spec Commands" href="/cli/commands/spec">
    List and inspect infrastructure configurations.
  </Card>
  <Card title="Plan & Apply" href="/cli/commands/plan-and-apply">
    Queue plan, deploy, and destroy jobs.
  </Card>
  <Card title="Job Commands" href="/cli/commands/jobs">
    List, stream logs, cancel, and wait for jobs.
  </Card>
  <Card title="Runner Commands" href="/cli/commands/runners">
    Register, list, and remove Runners.
  </Card>
  <Card title="Cluster Commands" href="/cli/commands/clusters">
    List provisioned Kubernetes clusters.
  </Card>
</Cards>

## Quick Start

```bash
# Install
brew install bobikenobi12/bb-thesis-2026/alethia

# Authenticate
alethia login

# Connect a cloud account (AWS, GCP, or Azure)
alethia connector gcp --project my-gcp-project

# List zones
alethia zone list

# Plan infrastructure
alethia plan --spec-id <id>

# Stream logs
alethia jobs logs --id <job-id> --follow

# Deploy
alethia apply --spec-id <id> --plan-job-id <plan-job-id>
```
