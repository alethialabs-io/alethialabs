---
title: Design a Project Overview
description: The multi-section infrastructure configuration form with Provider Ribbon and Cloud Identity.
---

# Design a Project

The Design a Project form is the primary way to define infrastructure. It has 11 configurable sections, a Provider Ribbon for switching between AWS/GCP/Azure, a Cloud Identity selector, and a real-time cost sidebar.

## Form Structure

The form is built with `react-hook-form` + Zod validation, using `useFieldArray` for multi-instance components (databases, caches, etc.). Each section is an independent component that reads and writes to the shared form context.

## Provider Ribbon

The Provider Ribbon sits at the top of the form and controls three things:

1. **Cloud Identity Selector** — dropdown showing connected cloud accounts. Each account displays its provider icon, name, and account identifier (AWS Account ID, GCP Project ID, Azure Subscription).

2. **Region Selector** — populated with the selected provider's available regions, grouped by geography (US, Europe, Asia Pacific, etc.).

3. **Existing resources** — VPCs, subnets, and other resources you can reuse come from Alethia's
   server-side [asset inventory](/console/connectors/health-and-inventory), which stays current
   automatically (event-driven + a reconciliation sweep). There's no manual refresh.

When the Cloud Identity changes (e.g., switching from an AWS account to a GCP project), the entire form adapts. See [Cloud Provider Abstraction](/concepts/cloud-abstraction) for how this works.

<Callout type="warn">
Switching providers resets all provider-specific defaults: instance types, K8s versions, database engines, cache node types, and DNS/WAF options. If you're duplicating a project across providers, the platform converts the config automatically with warnings. See [Multi-Cloud Conversion](/concepts/multi-cloud-conversion).
</Callout>

## Sections

<Cards>
  <Card title="Project Basics" href="/console/design-project/project-basics">Name, environment, OpenTofu version</Card>
  <Card title="Network" href="/console/design-project/network">VPC/VNet, CIDR, NAT gateway</Card>
  <Card title="Cluster" href="/console/design-project/cluster">K8s, instance types, node scaling, Karpenter</Card>
  <Card title="Databases" href="/console/design-project/databases">Aurora, Cloud SQL, Azure Database</Card>
  <Card title="Caches" href="/console/design-project/caches">Redis, Valkey, Memorystore</Card>
  <Card title="NoSQL" href="/console/design-project/nosql">DynamoDB, Firestore, Cosmos DB</Card>
  <Card title="Messaging" href="/console/design-project/messaging">SQS, Pub/Sub, Service Bus</Card>
  <Card title="DNS" href="/console/design-project/dns">Route53, certificates, WAF</Card>
  <Card title="Secrets" href="/console/design-project/secrets">Password generation, secret stores</Card>
  <Card title="Container Registry" href="/console/design-project/container-registry">ECR, Artifact Registry, ACR</Card>
  <Card title="Repositories" href="/console/design-project/repositories">Git repos for GitOps</Card>
  <Card title="Cost Sidebar" href="/console/design-project/cost-sidebar">Real-time pricing estimates</Card>
</Cards>

## Review & Submit

After filling in sections, the **Review Tab** shows a read-only summary of the entire configuration organized by category. On submit:

1. A `projects` row is created with status `DRAFT`
2. Component rows are created in the relevant tables (`project_network`, `project_cluster`, `project_databases`, etc.)
3. The project appears in the project listing
4. The user can now run Plan → Apply from the [project detail page](/console/dashboard/project-detail)

## Form Validation

Each section has its own Zod schema. Key validations:

- Project name: lowercase, alphanumeric + hyphens, max 25 characters
- CIDR blocks: regex validation + numeric range check
- Node sizing: min ≤ desired ≤ max
- Instance types: max 5 selected
- Database names: unique per project, lowercase
- Secret names: alphanumeric + hyphens only

On submit failure, the form auto-scrolls to the first error field.
