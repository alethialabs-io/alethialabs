packages:
  - "apps/*"
  - "packages/*"
  # Enterprise edition (commercial license). Optional: the community build omits it;
  # apps/console loads it at runtime via createRequire when present.
  - "ee"
