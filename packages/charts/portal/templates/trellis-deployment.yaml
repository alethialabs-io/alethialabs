apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "portal.fullname" . }}-trellis
  labels:
    {{- include "portal.labels" . | nindent 4 }}
    app.kubernetes.io/component: trellis
spec:
  replicas: {{ .Values.trellis.replicaCount }}
  selector:
    matchLabels:
      {{- include "portal.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: trellis
  template:
    metadata:
      labels:
        {{- include "portal.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: trellis
    spec:
      serviceAccountName: {{ include "portal.serviceAccountName" . }}
      containers:
        - name: trellis
          image: "{{ .Values.trellis.image.repository }}:{{ .Values.trellis.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.trellis.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "portal.fullname" . }}-secrets
          {{- with .Values.trellis.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.trellis.resources | nindent 12 }}
