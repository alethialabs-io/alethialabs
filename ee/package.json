{
	"name": "@alethia/ee",
	"version": "0.1.0",
	"private": true,
	"description": "Alethia Enterprise Edition — orgs, SSO, OpenFGA-backed RBAC. Commercial license (see ./LICENSE). Imports the AGPL core via its seams; the core never imports this.",
	"license": "LicenseRef-Alethia-Commercial",
	"main": "./dist/index.js",
	"exports": {
		".": {
			"default": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "esbuild src/index.ts --bundle --platform=node --format=cjs --packages=external --outfile=dist/index.js",
		"check-types": "tsc -p tsconfig.json --noEmit"
	},
	"peerDependencies": {
		"@better-auth/sso": "^1.6.0",
		"@openfga/sdk": "^0.8.0",
		"better-auth": "^1.6.19",
		"drizzle-orm": "^0.45.2",
		"jose": "^5.0.0",
		"samlify": "^2.0.0"
	},
	"devDependencies": {
		"esbuild": "^0.21.5",
		"typescript": "^5"
	}
}
