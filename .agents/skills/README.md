# Alethia shared Codex skills

The skill directories beside this file are links to `.claude/skills/`, which remains the
single source of truth for Alethia workflows used by Claude and Codex.

Do not edit a linked skill here. Update the source under `.claude/skills/` and run
`pnpm codex:doctor` to verify that the links remain intact.
