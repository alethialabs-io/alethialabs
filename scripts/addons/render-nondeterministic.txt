# Add-ons whose charts render DIFFERENTLY every time, and so sit permanently OutOfSync under
# ArgoCD. See scripts/addons/check-render-determinism.sh for what that costs.
#
# THIS LIST IS A RATCHET. The check fails if an entry here becomes deterministic, so a fix cannot
# land without also shrinking the list. Add a line only with the issue that tracks its removal.

harbor   # 2823 — genCA/genSignedCert for expose.tls AND an EPHEMERAL core token-signing keypair,
         #        plus randAlphaNum for core.secret/xsrfKey, jobservice.secret, registry.secret and
         #        a per-render bcrypt salt in the registry htpasswd. The rotating checksum/secret
         #        annotations roll core, jobservice, registry and trivy on every reconcile.
minio    # 2822 — randAlphaNum rootUser + rootPassword whenever existingSecret is unset.
