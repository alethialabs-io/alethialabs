{
	"_comment": [
		"The board's label set — one source, read by both consumers.",
		"",
		"`coordinate.sh --init-labels` mints every entry here. `decompose-validate.mjs` derives the",
		"label names a proposal may carry from the same file. They used to be two hand-written lists",
		"that had to agree, and they stopped agreeing: seven program waves (fabric, canvas, frontdoor,",
		"connectors-v2, capabilities, compat, offer-parity) existed on GitHub and were in use, while",
		"the validator rejected all of them as `unknown label` and a fresh repo would never create",
		"them. A decomposition for any of those programs could not be seeded.",
		"",
		"`kind` decides how each consumer treats a label:",
		"  wave | lane | class  — a proposal must carry exactly one of each",
		"  authorable           — a proposal MAY carry it (mutex/needs)",
		"  runtime              — maintained by claim-work.sh / coordinate.sh; a proposal must NOT",
		"                         pre-set it, or it corrupts the claim/blocked bookkeeping",
		"  board                — minted for the board, never valid on a proposed unit (e.g. `epic`,",
		"                         which is an umbrella and is never directly built or claimed)",
		"",
		"Adding a program wave is one entry here — nothing else to keep in step.",
		"",
		"THIS FILE OVERWRITES THE BOARD. `--init-labels` mints with `gh label create --force`, which",
		"UPDATES an existing label rather than skipping it, so an entry here is not a default — it is",
		"the value the label WILL have. Getting that wrong is not theoretical: on 2026-09-01 the six",
		"waves below were added with freshly-written descriptions, `--init-labels` was run, and four",
		"live labels lost their W-number prefix (W0/W2/W3/W5) while all six were flattened to one",
		"colour. The board carries a SEPARATE wave:W1..W7 numbering, so those prefixes were the only",
		"thing tying the named tracks to it. Restored below from the values recorded in the review of",
		"#3610. When adding a label that already exists on GitHub, copy its live description and",
		"colour verbatim (`gh label list --json name,description,color`) rather than writing new ones.",
		"",
		"It drifted a SECOND time, in the other direction: on 2026-09-01 six wave labels were live on",
		"GitHub (e2e-green, byoiac-ga, cli-first, truth, elench-repo, hardening) that this file did not",
		"name, so the validator would have rejected a decomposition onto any of them. Collapsing two",
		"lists into one fixed the two consumers agreeing with EACH OTHER; it does not make either agree",
		"with the board, because `gh label create` still works. This file is the source — mint a wave",
		"HERE and let `--init-labels` push it, never the other way round.",
		"",
		"It drifted a THIRD time, the same direction, and the sentence above is why: `release-gate:all`",
		"was created with `gh label create` while building #4440, `release-gate.yml` has read it ever",
		"since, and this file had never heard of it — so `--init-labels` on a fresh clone would not have",
		"minted it and the escape hatch would silently not exist (#4537). Added below, description and",
		"colour copied verbatim from the live label per the paragraph above.",
		"",
		"WHY NOTHING RECONCILES THE TWO, and what a check would have to know. Measured 2026-09-09: 68",
		"labels live on the repo, 41 named here. The other 27 are not drift and must NOT be minted from",
		"here — GitHub's nine defaults, release-please's `autorelease:*`, dependabot's ecosystem labels,",
		"Mergify's `queued`/`dequeued`, and the bot-upserted dedup identities (`from:*`, `tracker:*`,",
		"`security`). So a check that simply diffed this file against `gh label list` would red on 26",
		"labels it has no business owning, and the first thing anyone would do is silence it. The",
		"reconciler #4537 asks for therefore needs a stated notion of WHICH labels this file governs,",
		"and that is a new script and a separate unit — not a line in this one. Left undone deliberately.",
		"In the direction that CAN be checked cheaply today, the two agree: on 2026-09-09 no entry here",
		"differed in colour or description from its live label, so `--init-labels` would clobber nothing."
	],
	"labels": [
		{ "name": "wave:W1", "kind": "wave", "color": "1d76db", "description": "north-star wave 1" },
		{ "name": "wave:W2", "kind": "wave", "color": "1d76db", "description": "north-star wave 2" },
		{ "name": "wave:W3", "kind": "wave", "color": "1d76db", "description": "north-star wave 3" },
		{ "name": "wave:W4", "kind": "wave", "color": "1d76db", "description": "north-star wave 4" },
		{ "name": "wave:W5", "kind": "wave", "color": "1d76db", "description": "north-star wave 5" },
		{ "name": "wave:W6", "kind": "wave", "color": "1d76db", "description": "north-star wave 6" },
		{ "name": "wave:W7", "kind": "wave", "color": "1d76db", "description": "north-star wave 7" },
		{ "name": "wave:hygiene", "kind": "wave", "color": "0e8a16", "description": "launch-hygiene track (parallel)" },
		{ "name": "wave:fabric", "kind": "wave", "color": "1d76db", "description": "program track: Fabric / Placement" },
		{ "name": "wave:canvas", "kind": "wave", "color": "1d76db", "description": "program track: canvas overhaul" },
		{ "name": "wave:frontdoor", "kind": "wave", "color": "1d76db", "description": "program track: front door" },
		{ "name": "wave:connectors-v2", "kind": "wave", "color": "1d76db", "description": "program track: connectors v2" },
		{ "name": "wave:capabilities", "kind": "wave", "color": "1d76db", "description": "program track: cloud capabilities catalog" },
		{ "name": "wave:compat", "kind": "wave", "color": "1d76db", "description": "program track: version-compatibility matrix" },
		{ "name": "wave:offer-parity", "kind": "wave", "color": "1d76db", "description": "program track: offer parity" },
		{ "name": "wave:e2e-green", "kind": "wave", "color": "0e8a16", "description": "W2 e2e green: floor+full-bar+FULLY-TESTED across 5 clouds" },
		{ "name": "wave:byoiac-ga", "kind": "wave", "color": "1d76db", "description": "W3 BYO-IaC GA: sandbox canary + continuous multi-stack loop" },
		{ "name": "wave:cli-first", "kind": "wave", "color": "fbca04", "description": "W5 CLI-first: declarative import + tier-1 write routes + auth fixes" },
		{ "name": "wave:truth", "kind": "wave", "color": "d93f0b", "description": "W0 truth & safety: verify/iacsafety gates tell the truth" },
		{ "name": "wave:elench-repo", "kind": "wave", "color": "5319e7", "description": "W4 Elench analyzes your repo: repo→plan glue + alethia verify + Action" },
		{ "name": "wave:hardening", "kind": "wave", "color": "c2e0c6", "description": "W6 tenancy · rate limits · receipts" },
		{ "name": "wave:release-gate", "kind": "wave", "color": "0e8a16", "description": "program track: the browser release gate — Playwright in Actions on staging→main + prod QA pass" },
		{ "name": "wave:console-ui", "kind": "wave", "color": "1d76db", "description": "program track: console UI conformance — every private page measured, then lifted" },

		{ "name": "lane:schema", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: schema" },
		{ "name": "lane:server", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: server" },
		{ "name": "lane:runner", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: runner" },
		{ "name": "lane:core", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: core" },
		{ "name": "lane:canvas", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: canvas" },
		{ "name": "lane:console", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: console pages/components EXCLUDING the canvas subtree (lane:canvas)" },
		{ "name": "lane:tests", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: tests" },
		{ "name": "lane:docs", "kind": "lane", "color": "5319e7", "description": "file-ownership lane: docs" },

		{ "name": "class:backend", "kind": "class", "color": "0e8a16", "description": "autonomous: claim→PR→enqueue on green (merge queue)" },
		{ "name": "class:ui", "kind": "class", "color": "d93f0b", "description": "human-in-loop: design-spec → Claude Design → gated merge" },

		{ "name": "mutex:migration", "kind": "authorable", "color": "e99695", "description": "generates a drizzle migration — serialized, one at a time" },
		{ "name": "needs:design", "kind": "authorable", "color": "d4c5f9", "description": "UI unit awaiting the Claude-Design build" },
		{ "name": "needs:human", "kind": "authorable", "color": "d4c5f9", "description": "awaiting a human decision/gate" },

		{ "name": "claimed", "kind": "runtime", "color": "fbca04", "description": "held by an instance (carries a lease comment)" },
		{ "name": "blocked", "kind": "runtime", "color": "b60205", "description": "a blocked-by dependency is still open (coordinate-maintained)" },

		{ "name": "release-gate:run", "kind": "board", "color": "0e8a16", "description": "run the browser release gate on this dev PR (release-gate.yml); never on a proposed unit" },
		{ "name": "release-gate:all", "kind": "board", "color": "0e8a16", "description": "run EVERY release-gate leg on this dev PR, overriding path selection; never on a proposed unit" },
		{ "name": "qa:prod", "kind": "board", "color": "5319e7", "description": "a defect or report from the /console-prod-qa pass on alethialabs.io; never on a proposed unit" },
		{ "name": "epic", "kind": "board", "color": "8b5cf6", "description": "umbrella/tracking issue — decomposed into sub-issues, never directly built/claimed" }
	]
}
